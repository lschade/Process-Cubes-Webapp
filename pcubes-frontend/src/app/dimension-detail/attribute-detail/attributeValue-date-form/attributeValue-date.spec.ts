import { AttributeValueDate } from './attributeValue-date';

describe('AttributeValueDate', () => {
  it('should create an instance', () => {
    expect(new AttributeValueDate()).toBeTruthy();
  });
});
