import { AttributeValueNumber } from './attributeValue-number';

describe('AttributeValueNumber', () => {
  it('should create an instance', () => {
    expect(new AttributeValueNumber()).toBeTruthy();
  });
});
