import { DimensionAttribute } from './dimension-attribute';

describe('DimensionAttribute', () => {
  it('should create an instance', () => {
    expect(new DimensionAttribute()).toBeTruthy();
  });
});
